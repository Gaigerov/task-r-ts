export enum TaskStatus {
    Pending = 'pending',
    InWork = 'inWork',
    Done = 'done',
}

export enum PageName {
    Main = 'main',
    Edit = 'edit',
}

export enum StoreKey {
    Tasks = 'gaiger-tasks',
}

export enum FormStatus {
    Create = 'create',
    Edit = 'edit',
}
